## UMP Bot — Telegram‑бот для мониторинга ТС в парках (ТЗ + эксплуатация)

### 1) Назначение
Проект предоставляет Telegram‑бота, который:
- принимает от пользователя **списки борт/депо‑номеров ТС** (вручную командой или через текст с категориями),
- получает по ним данные из UMP (позиция/статус),
- формирует **карту парка** (изображение) с точками ТС,
- позволяет выбрать парк и проверять состояние отдельного ТС,
- даёт диагностику по филиалу (ошибки оборудования),
- содержит **админ‑панель** (внутри Telegram) для мониторинга состояния бота.

### 2) Роли и доступ
- **Обычный пользователь**: может пользоваться командами бота в рамках ограничений доступа.
- **Администратор**: **только Telegram user id `<id>>`**. Имеет доступ к `/admin` и кнопкам админ‑панели.

#### 2.1 Ограничение доступа к боту
Доступ к основным командам контролируется переменной окружения `TELEGRAM_ALLOWED_USERS`.
- Если `TELEGRAM_ALLOWED_USERS` **пустая** — доступ **открыт всем**.
- Если заполнена — доступ разрешён только перечисленным user id.

> Админ‑панель не зависит от `TELEGRAM_ALLOWED_USERS`: она доступна **исключительно** `<id>>`.

### 3) Пользовательская функциональность (что делает бот)

#### 3.1 Команды
- **`/start`** — старт, краткая справка. При отсутствии токена запускается предложение авторизации.
- **`/help`** — справка.
- **`/login`** — запуск диалога авторизации в UMP (логин → пароль).
- **`/parks`** — выбор парка через inline‑кнопки.
- **`/status <номер>`** — проверка статуса конкретного ТС.
- **`/map <номер1> <номер2> ...`** — построение карты только по явно переданным номерам.
- **Текст без команды** — парсинг текста «категория: номера» и построение карты с раскраской.
- **`/diag <филиал>`** — диагностика по филиалу (ошибки оборудования).
- **`/test`** — служебная команда диагностики конфигурации/файлов/токена.
- **`/admin`** — админ‑панель (только `825719797`).

#### 3.2 Сценарии работы

##### Сценарий A: первичный запуск и авторизация
1) Пользователь пишет `/start`.
2) Если токена UMP для пользователя нет — бот предлагает авторизоваться.
3) Пользователь пишет `/login`.
4) Бот запрашивает логин UMP, затем пароль.
5) После успешного логина:
   - токен сохраняется в `var/user_tokens/<user_id>_token.txt`,
   - cookies сохраняются в `var/user_cookies/<user_id>_cookies.txt`,
   - учётные данные сохраняются в `var/user_creds/<user_id>_creds.json` (для автологина).

##### Сценарий B: карта по списку номеров
1) Пользователь вызывает `/map 6683 6719 6306`.
2) Бот:
   - проверяет доступ,
   - проверяет/обновляет UMP‑сессию пользователя,
   - валидирует номера,
   - получает данные по ТС,
   - рендерит карту и отправляет изображение.

Важно: `/map` **без аргументов** не читает файл номеров. В этом режиме бот просит указать номера или отправить текст без команды.

##### Сценарий C: карта по тексту с категориями
1) Пользователь отправляет текст (без команды), например:

```
Заявки Redmine:
6510
6241

Текущие задачи:
6719
6684
```

2) Бот:
   - парсит категории/номера,
   - строит цветовую карту категорий,
   - строит карту с раскраской точек по категориям,
   - отправляет изображение.

##### Сценарий D: статус ТС
1) Пользователь вызывает `/status 6569`.
2) Бот проверяет токен, при 401 пытается автологин по сохранённым кредам.
3) Возвращает статус «в парке/вне парка», парк, координаты и время.

##### Сценарий E: диагностика филиала
1) Пользователь вызывает `/diag Екатерининский`.
2) Бот:
   - сопоставляет имя филиала с id через `UMP_BRANCH_MAP`,
   - получает диагностические данные,
   - фильтрует/форматирует «красные» проблемы,
   - отправляет результат.

### 4) Админ‑панель (только `<id>`)
Команда: **`/admin`**.

Кнопки:
- **📊 Статистика**: количество сессий в памяти, выбранный парк, количество файлов токенов/куков/кредов/мета, состояние вашего токена.
- **🔑 Токены/файлы**: наличие ключевых env (без раскрытия секретов), сводка по файлам токенов/куков/кредов.
- **🧾 Логи (tail)**:
  - читает лог‑файл (по умолчанию `var/ump_bot.log`, настраивается `UMP_BOT_LOG_FILE`),
  - если файла нет — пытается взять последние строки через `journalctl -u ump-bot` (если доступно).
- **📦 Окружение**: Python/OS, hostname, docker‑детект, uptime (если есть `/proc/uptime`), loadavg, диск, `CACHE_DIR`, статус `systemctl is-active ump-bot` (если доступно).
- **🌐 UMP healthcheck**: быстрый HTTP‑запрос к `UMP_BASE_URL`.

Ограничения:
- в Docker‑контейнере обычно **нет systemd/journalctl**, поэтому просмотр логов лучше делать через `docker compose logs`.

### 5) Архитектура (как это сделано)

#### 5.1 Точки входа
- `bot.py` — запускает бота и перезапускает при падении.
- `src/ump_bot/telegram_bot.py` — создаёт `Application` (python‑telegram‑bot) и регистрирует handlers.

#### 5.2 Основные модули
- `src/ump_bot/handlers/*` — обработчики команд Telegram.
- `src/ump_bot/services/auth.py` — авторизация пользователей в UMP, хранение токенов/кредов, автологин.
- `src/ump_bot/infra/login_token.py` — низкоуровневый логин в UMP (`/api/v1/auth/login`).
- `src/ump_bot/services/map.py` + `src/ump_bot/infra/render_map.py` — рендер карты (тайлы, точки, цвета).
- `src/ump_bot/infra/otbivka.py` — получение данных/позиции ТС и загрузка парков.
- `src/ump_bot/diagnostic.py` / `src/ump_bot/services/diagnostic.py` — логика диагностики по филиалам.
- `src/ump_bot/config/*` — конфигурация, чтение `.env`, дефолты.

#### 5.3 Хранение пользовательских данных
По умолчанию всё лежит в `var/` (директория в `.gitignore`).
- `var/user_tokens/<user_id>_token.txt`
- `var/user_cookies/<user_id>_cookies.txt`
- `var/user_creds/<user_id>_creds.json`
- `var/user_meta/*` (зарезервировано)
- `var/cache` и др. кэши (см. переменные ниже)

### 6) Конфигурация (переменные окружения)
Конфигурация читается через `.env` (pydantic‑settings).

#### 6.1 Telegram
- `TELEGRAM_BOT_TOKEN` — токен бота.
- `TELEGRAM_ALLOWED_USERS` — список user id через запятую (пусто = доступ открыт).

#### 6.2 UMP
- `UMP_BASE_URL` (по умолчанию `http://ump.piteravto.ru`)
- `REQUEST_TIMEOUT` (по умолчанию `20`)
- `UMP_TIMEZONE_OFFSET` (по умолчанию `180`)
- `UMP_USER_ID` — user_id для диагностики (если нельзя извлечь из токена).
- `UMP_BRANCH_MAP` — карта филиалов для `/diag`, например:

```
UMP_BRANCH_MAP={"Екатерининский":1382,"...":1234}
```

> Дополнительно есть `UMP_USER` / `UMP_PASS` — используются CLI‑входом `src/ump_bot/infra/login_token.py` (не обязателен для обычной работы через `/login`).

#### 6.3 Файлы/каталоги
- `PARKS_FILE` (по умолчанию `src/ump_bot/data/parks.json`)
- `VEHICLES_FILE` (по умолчанию `src/ump_bot/data/vehicles.sample.txt`)
- `USER_TOKEN_DIR` (по умолчанию `var/user_tokens`)
- `USER_COOKIES_DIR` (по умолчанию `var/user_cookies`)
- `USER_CREDS_DIR` (по умолчанию `var/user_creds`)
- `USER_META_DIR` (по умолчанию `var/user_meta`)

#### 6.4 Карта
- `MAP_PROVIDER` — URL тайлов (может содержать `{apikey}`)
- `MAPTILER_API_KEY` — ключ провайдера
- `MAP_USER_AGENT`, `MAP_REFERER`
- `MAP_CACHE_DIR` (по умолчанию `var/tile_cache`)
- `MAP_OUT_DIR` (по умолчанию `out`)
- `MAP_TPS` (по умолчанию `3.0`) — ограничение скорости загрузки тайлов
- `MAP_ZOOM` (по умолчанию `17`)
- `MAX_IMAGE_SIZE_MB` (по умолчанию `10`)

#### 6.5 Кэш/стабильность
- `CACHE_DIR` (по умолчанию `var/cache`)
- `CACHE_TTL` (по умолчанию `120`)
- `ANTI_FLAP_GRACE_M` (по умолчанию `3.0`)

#### 6.6 Логи
- `LOG_LEVEL` (по умолчанию `INFO`)
- `UMP_BOT_LOG_FILE` — путь к лог‑файлу для админки (опционально).

### 7) Требования к окружению
- Python (локально): совместимая версия (проект использует `python-telegram-bot==21.*`).
- Docker: образ строится на `python:3.11-slim`.

### 8) Запуск

#### 8.1 Локально
```bash
python3 -m venv .venv
./.venv/bin/pip install -r requirements.txt

# создать .env (пример ниже)
./.venv/bin/python bot.py
```

#### 8.2 Docker
```bash
docker compose up -d --build
# логи
docker compose logs -f --tail=200
```

#### 8.2.0 Рекомендуемый способ управления (и чтобы “всегда работало”)
Чтобы не ловить ошибки вида **“no compose.yaml … file found”** (когда команда запущена не из каталога репозитория) и чтобы одинаково работало на Docker/Podman, используйте скрипты:

```bash
chmod +x ./scripts/compose.sh ./update_bot.sh

# запуск
./scripts/compose.sh up -d --build

# статус
./scripts/compose.sh ps

# логи
./scripts/compose.sh logs -f --tail=200 ump-telegram-bot

# обновление (pull + rebuild + restart)
./update_bot.sh
```

#### 8.2.1 Troubleshooting (Podman / podman-compose)
На некоторых серверах команда `docker` может быть **эмуляцией Podman** (в выводе будет что-то вроде “Emulate Docker CLI using podman”, а `docker compose` будет вызывать `podman-compose`).

- **`ERRO ... acquiring lock ...: file exists` / `retrieving lock ...: file exists`**: обычно означает “залипший” lock после аварийного завершения podman-процесса или параллельных операций (`docker compose ...` в двух терминалах, авто-скрипт обновления + ручной запуск).
  - Проверьте, нет ли параллельных процессов: `pgrep -af podman`
  - Посмотрите поды/контейнеры: `podman ps -a` и `podman pod ps`
  - Если lock мешает управлению и нет активных podman-процессов, чаще всего помогает перезапуск хоста (rootless runtime хранит lock-и в `/run/user/<uid>`).

- **`Exited (137)` у контейнера**: контейнер был убит сигналом `SIGKILL` (частая причина — **OOM-kill** на слабом сервере).
  - Логи контейнера: `podman logs ump-telegram-bot --tail=200`
  - Проверка OOM в состоянии контейнера: `podman inspect ump-telegram-bot --format '{{.State.OOMKilled}} {{.State.ExitCode}} {{.State.Error}}'`
  - Проверка OOM в ядре: `journalctl -k --since "1 hour ago" | grep -i oom` (или `dmesg | grep -i oom`)

#### 8.2.2 Автозапуск Podman после ребута/логаута (rootless)
Если вы запускаете rootless Podman под пользователем `secureadmin`, контейнер может не стартовать сам после ребута, пока вы не зайдёте по SSH.
Решение — включить linger и поставить user-unit:

```bash
# один раз, от root:
sudo loginctl enable-linger secureadmin

# от secureadmin:
mkdir -p ~/.config/systemd/user
cp -f ~/ump-bot/deploy/ump-bot-podman.service ~/.config/systemd/user/ump-bot-podman.service
systemctl --user daemon-reload
systemctl --user enable --now ump-bot-podman.service
systemctl --user status ump-bot-podman.service
```

Примечание: в логах у вас уже светился токен бота Telegram. Если он реальный/боевой — лучше **перегенерировать токен** в BotFather и обновить `.env`.

#### 8.3 systemd
В репозитории есть пример `ump-bot.service`.
- статус: `systemctl status ump-bot`
- логи: `journalctl -u ump-bot -f`

### 9) Пример `.env` (шаблон)
```bash
# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USERS=

# UMP
UMP_BASE_URL=http://ump.piteravto.ru
REQUEST_TIMEOUT=20
UMP_TIMEZONE_OFFSET=180
# UMP_USER_ID=...
# UMP_BRANCH_MAP={"Екатерининский":1382}

# Файлы
PARKS_FILE=src/ump_bot/data/parks.json
VEHICLES_FILE=src/ump_bot/data/vehicles.sample.txt

# Карта (пример для MapTiler)
MAPTILER_API_KEY=
MAP_PROVIDER=https://api.maptiler.com/maps/streets-v2/256/{z}/{x}/{y}.png?key={apikey}
MAP_ZOOM=17
MAP_TPS=3
MAP_CACHE_DIR=var/tile_cache
MAP_OUT_DIR=out
MAX_IMAGE_SIZE_MB=10
# MAP_USER_AGENT=UMPBot/1.0
# MAP_REFERER=

# Логи (если хотите читать из файла в админке)
# UMP_BOT_LOG_FILE=var/ump_bot.log

LOG_LEVEL=INFO
```

### 10) Тесты
```bash
./.venv/bin/python -m pytest -q
```

### 11) Нефункциональные требования (эксплуатационные)
- **Безопасность**: не хранить `.env` в git; ограничивать доступ `TELEGRAM_ALLOWED_USERS`; помнить, что креды UMP сохраняются в `var/user_creds`.
- **Наблюдаемость**: логи через stdout/stderr (Docker/systemd), либо в файл при необходимости.
- **Устойчивость**: `bot.py` перезапускает бота при падении.

### 12) Границы ответственности (что проект НЕ делает)
- Не предоставляет веб‑интерфейс (всё управление через Telegram).
- Не гарантирует доступность UMP (зависит от сети/UMP).
- Не хранит историю запросов в БД (только файлы в `var/`).

---

## Планы / Roadmap: “Таблица смены” (/table)

### Контекст
Сейчас бот умеет:
- принимать текст с секциями (заголовки вида `...:`) и номерами ТС,
- строить карту с раскраской по категориям (секциям),
- проверять статус отдельного ТС (`/status`) и строить карту по явному списку (`/map`).

Нужно добавить новый сценарий: **однократно за смену** пользователь вставляет “таблицу” задач (секциями), бот сохраняет её **персонально для каждого пользователя**, и дальше с ней можно работать (просмотр/карта/удаление, карточки задач и т.д.).

### Исходный запрос (как сформулировано пользователем)

Пользовательский ввод (пример 1):

```
Заявки Redmine:
6498	ТС валидатор Заявка #594101
6702	ТС Монитор Заявка #594152
6831	ТС Отбивка Заявка #594315
6501	ТС ЦБК и Телтоника Заявка #594425

Текущие задачи:
6693	архивы

Проверка ГК, маршрут 485, 469:
6825
10123
10187
10127
```

Пользовательский ввод (пример 2):

```
Заявки Redmine:
6700	ТС  Телтоника Заявка #593649

Текущие задачи:
6559	Датчик температуры
6683	Проверить валидаторы
6693	Ошибка АСМПП
6698	Проверить валидаторы
6774	Проверить валидаторы
6515	Проверить валидаторы
6682	Боковое табло
6826	Салонное табло
10261	Ошибка по архивам в УМП ПОВТОР
```

Пояснение: сейчас бот воспринимает только номер (кроме заголовков), а нужно сохранять также текст задачи (“проблема/описание”).

Ожидаемый сценарий:
- После ввода таблицы пользователь получает сообщение “таблица заполнена”.
- Далее показывается выбор: **“Посмотреть таблицу”**, **“Посмотреть на карте”**, **“Удалить таблицу”**.
- “Посмотреть таблицу”: список с пагинацией по 20 записей; при выборе записи — карточка задачи:
  - Проблема (описание)
  - Статус ТС: “в парке / не в парке”
  - Действия: удалить/редактировать/сделано/комментарий/в процессе/показать на карте
- “Посмотреть на карте”: карта со всеми ТС из таблицы.
- “Удалить таблицу”: удаление сохранённой таблицы; далее требуется заново вводить таблицу.

Мультипользовательность:
- Ботом пользуется много людей — **данные таблицы должны храниться отдельно на каждого пользователя (по Telegram `user_id`)**.

### Принятые решения (зафиксировано)
1) **Импорт таблицы — только явно через `/table`** (чтобы не конфликтовать с текущим сценарием “просто отправил текст → сразу карта”).
2) **Разрешаем несколько задач на один номер ТС** (один ТС может встречаться в разных секциях/задачах).
3) **Одна активная таблица до удаления** (без авто‑сброса по дате/таймауту на первом этапе).
4) **Нужна раскраска по категориям на карте**, как сейчас в режиме текста с секциями:
   - при “Посмотреть на карте” (для всей таблицы) точки должны быть раскрашены по секциям (“Заявки Redmine”, “Текущие задачи”, …).
   - пример, где один номер встречается в нескольких секциях:

```
Заявки Redmine:
6704
6124
6198

Текущие задачи:


10271
6704

Проверка ГК, маршруты 255А, 255Б:
6303
6518
6514
10156
6515
6507
6510
6508
6509
6511
```

### Черновое ТЗ (детализация требований)

#### Цель
Добавить сущность “таблица смены” (активная таблица задач) и UX‑поток её использования:
- импорт таблицы одним сообщением в режиме `/table`,
- сохранение данных на диск в `var/` с привязкой к `user_id`,
- меню работы с таблицей,
- список задач с пагинацией,
- карточка задачи с действиями и интеграцией со статусом ТС/картой.

#### Формат ввода и парсинг
- Таблица — текст с секциями и строками задач.
- Заголовок секции:
  - строка с `:` (двоеточием) **или**
  - строка без цифр
  - имя секции = строка без завершающего `:` и с `strip()`
- Строка задачи:
  - начинается с валидного номера ТС (3–6 цифр),
  - далее может быть описание (через таб/пробелы), включая `#...` и любые символы
  - `number` = ведущие цифры в начале строки
  - `description` = остаток строки после номера (`strip()`), может быть пустым
- Пустые строки игнорируются.

#### Хранение данных (пер‑пользователь)
- Хранить по файлу на пользователя (файловая “мини‑БД” как уже сделано для токенов/доступа).
- Папка (предлагаемая): `var/user_meta/shift_tables/`
- Файл: `var/user_meta/shift_tables/<user_id>.json`
- Запись атомарная (`.tmp` + rename/replace).
- Структура: версия схемы + метаданные таблицы + массив задач (каждая со статусом, комментариями, timestamps).

#### Команды и UX
- `/table`:
  - если таблицы нет — бот просит прислать таблицу одним сообщением
  - если таблица есть — показывает меню: “Посмотреть таблицу / Посмотреть на карте / Удалить таблицу”
  - при повторном импорте — подтверждение “Перезаписать?”
- “Посмотреть таблицу”:
  - выдаёт список задач с пагинацией по 20
  - выбор задачи открывает карточку задачи
- Карточка задачи:
  - показывает “Проблема/описание”
  - показывает статус ТС “в парке/вне парка” (динамически из UMP)
  - кнопки: удалить/редактировать/сделано/комментарий/в процессе/показать на карте/назад
- “Посмотреть на карте”:
  - строит карту по всем уникальным ТС из таблицы
  - **раскраска по категориям/секциям** (как в текущем режиме секций)
  - если номер встречается в нескольких секциях: определить правило приоритета (например: “первое вхождение” или “последняя секция”), зафиксировать в реализации
- “Удалить таблицу”:
  - подтверждение, затем удаление файла таблицы пользователя

#### Ограничения и ошибки
- Если в импорте нет валидных номеров — сообщить об ошибке.
- Не делать массовые запросы в UMP при показе списка; статус ТС получать при открытии карточки (или по отдельной кнопке).
- Ограничение на количество ТС для карты сейчас 50 — в первом релизе сохраняем поведение (с предупреждением).
