# UMP Bot - Отслеживание ТС в парках

Бот для отслеживания транспортных средств в автобусных парках через Telegram.

## Возможности

- 📍 Интерактивная карта парка с ТС
- 🎨 Цветовое кодирование ТС по категориям задач
- 🏢 Выбор парка через кнопки
- 📊 Проверка статуса ТС
- 🔄 Автоматическая синхронизация с UMP

## Быстрый старт

### Локально (для разработки)

```bash
# Установка зависимостей
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настройка .env (см. DEPLOY.md)
cp .env.example .env
nano .env

# Авторизация в UMP
python login_token.py

# Запуск бота
python telegram_bot.py
```

### На сервере

См. подробную инструкцию в [DEPLOY.md](DEPLOY.md)

**Docker (рекомендуется):**
```bash
docker-compose up -d --build
```

**systemd:**
```bash
sudo systemctl start ump-bot
```

## Команды бота

- `/start` - Начать работу
- `/help` - Справка
- `/parks` - Выбрать парк
- `/map` - Карта с ТС из vehicles.txt
- `/map 6177 6848` - Карта для конкретных ТС
- `/status 6569` - Статус ТС

## Структура проекта

```
ump_bot/
├── telegram_bot.py      # Telegram бот
├── otbivka.py           # Логика работы с UMP
├── render_map.py        # Рендеринг карт
├── login_token.py       # Авторизация в UMP
├── config.py            # Конфигурация
├── parks.json           # Геозоны парков
├── vehicles.txt         # Список ТС с категориями
├── requirements.txt     # Зависимости
├── Dockerfile           # Docker образ
├── docker-compose.yml   # Docker Compose
├── ump-bot.service      # systemd service
└── DEPLOY.md            # Инструкция по деплою
```

## Оптимизация для слабого сервера

- Ограничение ресурсов (2 CPU, 1GB RAM)
- Кэширование тайлов и позиций ТС
- Оптимизированный размер изображений
- Настраиваемые параметры в .env

## Лицензия

MIT

